class Cart_Order {
  String itemId;
  String itemName;
  String imageUrl;
  String description;
  String quantity;
  String price;

  Cart_Order(this.itemId, this.itemName, this.imageUrl, this.description,
      this.quantity, this.price);
}
